target-version = "py311"
line-length = 100
# Applied migrations are immutable; do not rewrite historical revisions.
extend-exclude = ["alembic/versions"]

[lint]
select = ["E4", "E7", "E9", "F", "I"]

[lint.isort]
known-first-party = ["app"]
known-third-party = ["alembic"]
